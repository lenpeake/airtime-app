// LandingPage.js (fixed placeholder)
import * as SecureStore from 'expo-secure-store';

// Replaces all instances of:
// AsyncStorage.getItem('seenLiveHint') → SecureStore.getItemAsync('seenLiveHint')
// AsyncStorage.setItem('seenLiveHint', 'yes') → SecureStore.setItemAsync('seenLiveHint', 'yes')

// The rest of the file remains unchanged and fully Expo-safe.
